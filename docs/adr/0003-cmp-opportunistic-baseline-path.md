# Next alpha path is CMP opportunistic Form-4 baseline on SEC tape

After CFOB purchase-cluster E1 dual-exited **kill_line** on year-concentration (PR #78 / #76), the human chose the next alpha dollars for a **different** Form-4 object: Cohen–Malloy–Pomorski **opportunistic** open-market purchases, measured as a frozen baseline **before** any genetic/symbolic search. Data path is **free SEC EDGAR insider tape + entitled SEP only** — no SF1/SF2 prerequisite. GP/symbolic search (validated memo) is deferred to a **later PRD** only if this baseline clears power and regime gates.

## Considered options

- **CMP baseline first, then optional GP (chosen)** — strongest published prior still unmeasured as primary; reuses CFOB tape seams; kills cheaply if opportunistic mass is also 2009/2020 theater.
- **GP infrastructure first on synthetics only** — valid non-claim lab; rejected as the *sole* next alpha step because it delays the residual Form-4 claim.
- **Full GP proposal including Form-4+GP in one reopen** — rejected: optimizer must not redefine or rescue the event object; baseline replication first.
- **EVENTS-22 bridge** — cheaper entitled data; weaker prior; kept as optional later falsifier, not this path.
- **SF1+SF2 paid upgrade** — unlocks PEAD/quality and vendor insider tape; optional accelerator, not required for CMP on SEC tape.
- **Honest liquid beta productization only** — remains the capital default while research runs; not a substitute for the alpha search goal.

## Consequences

- #76 cluster kill stays settled; this line must not re-litigate cluster density or retune E1 gates.
- New event-only re-open is GitHub **#79** (CMP opportunistic purchase baseline; C+D authorised, E1 needs human go).
- SF1/SF2 entitlement failure does not block this path.
- Symbolic/GP search is out of scope until a separate authorisation after baseline stage_pass (or explicit kill of baseline).
