# Implementation Tasks: Sharadar Reference Data Adapters

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 700–1,000 (including reconciliation tests and boundary coverage) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 TICKERS reference facts (350–500) → PR 2 ACTIONS typed events (350–500) |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

## Apply-wide rules and evidence

- **Nothing below is complete.** The uncommitted reader modules, reader tests, and boundary test are scaffolding only; do not mark any implementation task complete or retain a preliminary behavior until its RED → GREEN → TRIANGULATE → REFACTOR evidence is recorded during apply.
- Use only `httpx.MockTransport` and injected `sleep`; no live HTTP call, credential, live marker, or paper-execution marker is permitted. Each cycle must record the focused test node/command, its failing pre-change result (RED), the minimal local change and passing result (GREEN), the distinct alternate/boundary example and passing result (TRIANGULATE), and the passing post-refactor command (REFACTOR).
- For every work unit, record `git diff --stat`, focused-test result, runtime-harness result (`N/A — injected MockTransport is the adapter runtime boundary`), and an independently stated rollback boundary before its work-unit commit. Keep tests with the code they prove; do not commit by file type.
- Reconciliation must preserve the approved intentional duplication: do **not** change `src/invest/adapters/sharadar_market_data.py` or extract shared transport plumbing in this change.

## PR 1 — TICKERS reference facts (target 350–500 changed lines)

**Start / finish / rollback:** Start from the interrupted `src/invest/adapters/sharadar_tickers.py`, `tests/adapters/test_sharadar_tickers.py`, and `tests/test_boundaries.py` without accepting their current results. Finish with one caller-supplied-client, fail-closed, deterministic TICKERS adapter and the ticker boundary guard. Roll back only those three file changes (and their PR-1 commit); no consumer wiring exists.

- [x] **1. Reconcile the TICKERS transport seam with vertical contract cycles** — In `tests/adapters/test_sharadar_tickers.py`, first add one public-interface RED test at a time for: required caller-supplied `httpx.Client` (no constructor-created transport), no-argument `fetch()`, fixed TICKERS `qopts.columns`, API-key query authentication, missing/empty `NASDAQ_DATA_LINK_API_KEY` failing as `MarketDataFetchError("auth-failure")` before `client.send`, and a key absent from captured logs/errors. Run each selected node and capture its failure against the preliminary implementation; make the smallest corresponding change in `src/invest/adapters/sharadar_tickers.py`, then rerun it GREEN. TRIANGULATE with both unset and empty credentials plus a second injected client/request shape; REFACTOR only local test fixtures/adapter helpers while the targeted file stays green. **Verification:** `pytest tests/adapters/test_sharadar_tickers.py`; `ruff check src/invest/adapters/sharadar_tickers.py tests/adapters/test_sharadar_tickers.py`. **Rollback:** remove only the reconciled TICKERS constructor/request tests and adapter changes.

- [x] **2. Reconcile validated TICKERS facts and deterministic pagination via vertical cycles** — Add RED→GREEN cycles in the same two files for: two-page cursor following and final `(ticker,)` sorting; nonempty continuation cursors only (reject empty, whitespace, non-string, or malformed metadata); `MAX_PAGES=512` continuation exhaustion; missing required columns, empty table, short row, and schema-invalid row; and a malformed second page proving `fetch()` raises `malformed-response` rather than returning an aggregate. TRIANGULATE each rule with a second page/order or alternate malformed row. Implement local envelope/cursor/row validation so a page is fully translated before aggregation and all failures use `MarketDataFetchError("malformed-response")`. **Verification:** focused parametrized nodes during each cycle, then `pytest tests/adapters/test_sharadar_tickers.py`. **Rollback:** remove only TICKERS pagination/validation changes; no SEP behavior changes.

- [x] **3. Reconcile TICKERS classification and listing-date translation via vertical cycles** — RED-test recognized domestic primary categories on each accepted exchange, a recognized non-primary, and unknown category/exchange; then minimally implement/retain the closed local mapping in `src/invest/adapters/sharadar_tickers.py`. TRIANGULATE a listed row with a provider last-price date (returned `delisted_date is None`) against a delisted row whose supplied last-price date is preserved, and a row with missing/empty classification fields that fails closed. Assert the frozen public `SharadarTicker` exposes only ticker, plain flags, and dates—not `category` or `exchange`. REFACTOR duplicated fixture construction without broadening the mapping. **Verification:** `pytest tests/adapters/test_sharadar_tickers.py -k 'metadata or classification or listed'`, then the complete TICKERS file. **Rollback:** revert the local mapping and associated tests only.

- [x] **4. Complete the TICKERS retry/error taxonomy with exact timing evidence** — In `tests/adapters/test_sharadar_tickers.py`, add one RED test at a time for 401/403 exactly once/no sleep; 429 exhaustion → `rate-limited`; 5xx, other HTTP error, and `httpx.RequestError` exhaustion → `network-failure`; exact three attempts and fallback sleeps `[0.5, 1.0]`; success after a retry; valid finite numeric `Retry-After` including negative/over-cap clamping; malformed, HTTP-date, `NaN`, and infinite headers falling back; and no sleep after the final attempt. GREEN each with the smallest local retry/backoff correction, TRIANGULATE each retry source with success-after-first-failure versus exhaustion, then REFACTOR only after all timing assertions pass. **Verification:** `pytest tests/adapters/test_sharadar_tickers.py -k retry`; then `pytest tests/adapters/test_sharadar_tickers.py`. **Rollback:** remove only TICKERS retry/backoff changes and tests.

- [x] **5. Establish the reusable AST isolation guard with the TICKERS case** — RED-test the absent generic/parametrized guard in `tests/test_boundaries.py`, then implement it in that file to reject identifier references plus `Import`/`ImportFrom` references to `SharadarTickersReader`/`invest.adapters.sharadar_tickers`. Its explicit protected discovery target must cover `src/invest/adapters/cli.py`, `src/invest/adapters/alpaca_broker.py`, `src/invest/application/execute_run.py`, `src/invest/application/scan_run.py`, `src/invest/domain/scanner.py`, `src/invest/domain/momentum_selection_scanner.py`, `src/invest/domain/market_context.py`, `src/invest/application/backtest_run.py`, and `src/invest/adapters/backtest_context_json.py`, plus any live/paper-reachable source discovered from the CLI entry points. TRIANGULATE with a synthetic forbidden AST/import fixture to prove the guard detects both direct and module-qualified use, then REFACTOR the existing five-path check into the generic helper without weakening the SEP guard. **Verification:** `pytest tests/test_boundaries.py -k sharadar`; `pytest tests/adapters/test_sharadar_tickers.py tests/test_boundaries.py`; `ruff check src tests`. **Rollback:** revert only the generic boundary helper and ticker parameter case.

- [x] **6. PR-1 review gate and scope check** — Before the PR-1 work-unit commit (suggested message: `feat(adapters): reconcile Sharadar ticker reference facts`), verify `git diff --stat` is 350–500 changed lines; if it is not, move only autonomous tests/behavior to PR 2 or split a new bounded PR rather than silently exceeding the slice. Run `pytest tests/adapters/test_sharadar_tickers.py tests/test_boundaries.py`, `pytest -m "not live and not paper_execute"`, and `ruff check src tests`. Inspect `git diff --name-only` and AST guard results to prove no edit or reference was added for `sharadar_market_data.py`, CLI selection/generator behavior, domain models, liquidity screening, `market_context.py`, `backtest_run.py`, `backtest_context_json.py`, execution, or live/paper paths. Record exact results and `N/A` mocked-runtime evidence. **Rollback:** revert the single PR-1 work unit.

## PR 2 — ACTIONS typed events (target 350–500 changed lines; stacks on PR 1)

**Start / finish / rollback:** Start after PR 1 with the preliminary `src/invest/adapters/sharadar_actions.py` and `tests/adapters/test_sharadar_actions.py` still unaccepted. Finish with a closed, Decimal-preserving ACTIONS contract and the second parameterized boundary case, without changing the stable ticker public contract. Roll back only ACTIONS module/test changes and the ACTIONS boundary parameter.

- [x] **7. Replace the preliminary raw ACTIONS shape through vertical typed-event cycles** — In `tests/adapters/test_sharadar_actions.py`, RED-test the public frozen `SharadarActionKind` values (`split`, `dividend`, `delisting`, `ticker-change`) and `SharadarAction(ticker, effective_date, kind, value)` contract; the test must fail against the current raw `date`/`action: str` scaffolding. GREEN the smallest local change in `src/invest/adapters/sharadar_actions.py` mapping exact provider literals `split`, `dividend`, `delisting`, and `tickerchange` (no case folding) to the closed enum. TRIANGULATE same ticker/date events with different kinds and a ticker-change mapping; REFACTOR local mapping/fixtures only after all pass. **Verification:** `pytest tests/adapters/test_sharadar_actions.py -k 'action or event'`, then the entire ACTIONS test file. **Rollback:** revert only the typed ACTIONS contract, mapping, and matching tests.

- [x] **8. Reconcile ACTIONS values, validation, pagination, and no-partial semantics** — Add RED→GREEN cycles in `tests/adapters/test_sharadar_actions.py` and `src/invest/adapters/sharadar_actions.py` for exact `Decimal` (never float) dividend/split values, absent `None` values for delisting/ticker-change, finite Decimal enforcement, positive split ratios, and unsupported action/invalid value rejection. Add equivalent fail-closed envelope tests: missing columns, empty table, short/invalid row, invalid/empty cursor metadata, page limit exhaustion, and a valid first page followed by malformed second page that returns no events. TRIANGULATE with equivalent values in different page orders and same ticker/date/kind values to prove stable `(ticker, effective_date, kind.value, stable-value-representation)` ordering independent of provider order. REFACTOR only after every validation and ordering test is green. **Verification:** focused nodes for each cycle, then `pytest tests/adapters/test_sharadar_actions.py`. **Rollback:** remove only ACTIONS parsing/validation/order changes and tests.

- [x] **9. Reconcile ACTIONS transport/retry behavior with the PR-1 taxonomy** — RED-test mandatory injected client, no-argument fetch, fixed ACTIONS requested columns, missing/empty key before send/no key in logs, and exact retry behavior: 401/403 one request; 429 `rate-limited`; 5xx, other HTTP errors, and `httpx.RequestError` `network-failure`; three-attempt limit; `[0.5, 1.0]` fallback; clamped finite numeric `Retry-After`; malformed/HTTP-date/`NaN`/infinite fallback; and no final-attempt sleep. GREEN each minimally without importing or altering SEP plumbing; TRIANGULATE retry-success and retry-exhaustion for each source; REFACTOR local ACTIONS helpers only. **Verification:** `pytest tests/adapters/test_sharadar_actions.py -k retry`; then `pytest tests/adapters/test_sharadar_actions.py`. **Rollback:** revert only ACTIONS request/retry changes and their tests.

- [x] **10. Prove ACTIONS price isolation and add the second AST guard case** — RED-test in `tests/adapters/test_sharadar_actions.py` that fetching valid actions returns only events and leaves independently supplied `DailyBar`/SEP-bar values unchanged; use an immutable before/after snapshot and MockTransport, not a live reader. GREEN by retaining an ACTIONS-only adapter with no SEP-bar import, adjustment, or mutation; TRIANGULATE with split and dividend fixtures. Then extend the PR-1 generic guard in `tests/test_boundaries.py` with `SharadarActionsReader`/`invest.adapters.sharadar_actions` across every protected path and the CLI/live/paper discovery set; test its detection with the same direct/module-qualified synthetic cases. **Verification:** `pytest tests/adapters/test_sharadar_actions.py tests/test_boundaries.py`; `ruff check src tests`. **Rollback:** remove only the no-adjustment regression and ACTIONS guard parameter.

- [x] **11. PR-2 review gate, complete regression, and explicit no-scope-drift check** — Before the PR-2 work-unit commit (suggested message: `feat(adapters): add typed Sharadar corporate actions`), confirm `git diff --stat` for the stacked slice is 350–500 changed lines and that PR 2 depends only on PR 1's boundary helper. Run `pytest tests/adapters/test_sharadar_tickers.py tests/adapters/test_sharadar_actions.py tests/test_boundaries.py`, `pytest -m "not live and not paper_execute"`, and `ruff check src tests`; record exact outputs and the mocked-runtime `N/A` evidence. Finally inspect `git diff --name-only`, `git diff`, and the AST guard to confirm no scope drift: no edits to `src/invest/adapters/sharadar_market_data.py`, `src/invest/domain/market_context.py`, `src/invest/application/backtest_run.py`, `src/invest/adapters/backtest_context_json.py`, domain models, CLI parsers/generators, liquidity screen, execution, live/paper paths, migrations, generated artifacts, or credentials. **Rollback:** revert PR 2 only; PR 1 remains a standalone TICKERS reference-data seam.
